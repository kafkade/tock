# apps/web

Next.js web client that consumes `tock-core` compiled to
`wasm32-unknown-unknown` (built with `wasm-pack`).

This directory is intentionally empty in the foundation phase. The web
client ships in Phase 6 of the roadmap — see
[`docs/architecture.md`](../../docs/architecture.md) §10 and ADR-005.
