# apps/macos

SwiftUI app for macOS. Shares the bulk of its code with `apps/ios` via
the UniFFI Swift bindings.

This directory is intentionally empty in the foundation phase. The app
ships in Phase 5 of the roadmap — see
[`docs/architecture.md`](../../docs/architecture.md) §10.
